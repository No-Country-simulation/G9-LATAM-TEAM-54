services:
  app-energi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: api-energi-ai
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped